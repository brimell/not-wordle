{
  "name": "server",
  "version": "1.0.0",
  "description": "backend for notwordle",
  "main": "server.js",
  "dependencies": {
    "@socket.io/admin-ui": "^0.2.0",
    "express": "^4.17.3",
    "express-rate-limit": "^6.2.0",
    "firebase": "^9.6.7",
    "socket.io": "^4.4.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku": "git add . && git commit -m 'heroku' && git push heroku master"
  },
  "author": "raaydon"
}
